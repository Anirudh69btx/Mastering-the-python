# Ask the user for a number. Print this number padded with leading zeros so the total width is exactly 5 digits
num=237
print(num.zfill(5))
#zfill() method pads a string with leading zeros (0) on the left until it reaches a specified width.
#string.zfill(width)