# Accept an integer from the user and display its value in binary format (Base 2).
num= int(input("enter the value of the num= "))
print(bin(num)) #The 0b prefix simply means "this number is in binary (Base 2)."