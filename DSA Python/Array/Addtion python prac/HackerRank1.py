#An array is a data structure that stores elements of the same type in a contiguous block of memory. In an array, A, of size N,each memory location has some unique index, i (where0 ≤ i < N), that can be referenced as A[i] or Aj.
#Your task is to reverse an array of integers.
def arr(array):
    